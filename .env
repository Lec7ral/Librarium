# Environment variables for local development.
# This file is loaded by the application on startup if it exists.
# It should NOT be committed to version control (add .env to your .gitignore file).

# --- Server Configuration ---
# The port the local server will listen on.
# Our config prioritizes the PORT variable from the hosting provider,
# so this will only be used if PORT is not set.
SERVER_PORT=8080

# --- Public Host Configuration for Swagger ---
# The public-facing URL for the API, used by Swagger UI.
# For local development, this is localhost. In production, your hosting
# provider should set PUBLIC_HOST and PUBLIC_SCHEME.
PUBLIC_HOST=localhost:8080
PUBLIC_SCHEME=http

# --- Database Configuration ---
DB_DSN=./library.db

# --- Redis Configuration ---
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# --- JWT Configuration ---
JWT_SECRET_KEY=a_secure_and_long_secret_for_local_development_that_is_not_the_default
